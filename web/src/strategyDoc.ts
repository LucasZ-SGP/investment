/**
 * Placeholder shown when the private repo has no strategy document yet.
 *
 * The real document deliberately does NOT live here. Anything in this bundle
 * is served from a public GitHub Pages site, so shipping the strategy as a
 * default would defeat the point of keeping it in a private repo at all.
 */
export const DEFAULT_STRATEGY_DOC = `# 尚未找到策略文档

私有仓库里没有 \`strategy.md\`。

把策略文档放到私有仓库根目录下的 \`strategy.md\`，刷新本页即可看到它。
支持标题、表格、列表、引用和代码块。

> 这份占位文本来自公开仓库的代码，因此不含任何策略内容。
> 实质内容只存在于你的私有仓库。
`;
